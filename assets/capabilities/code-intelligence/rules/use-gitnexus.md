# 使用语义索引规则

在以下场景中，必须先使用 GitNexus 语义索引工具，再进行代码操作：

1. **修改任何函数/类/方法前** — 运行 `gitnexus_impact` 分析影响范围
2. **理解不熟悉的代码时** — 使用 `gitnexus_query` 搜索相关执行流程
3. **提交代码前** — 运行 `gitnexus_detect_changes` 验证变更范围
4. **重命名符号时** — 使用 `gitnexus_rename` 而非手动查找替换

禁止在未进行影响分析的情况下直接修改核心模块代码。
